#if(count(resources) > 0):
<ul>
    #for(res in resources):
    <li>
        <a
            href="#(res.urlString)"
            rel="noopener noreferrer"
            target="_blank"
        >#(res.label)</a>
    </li>
    #endfor
</ul>
#endif
