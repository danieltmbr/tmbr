<div id="gallery-grid">
    #for(item in items):
    <button type="button" class="gallery-item" data-url="#(item.url)" data-alt="#(item.alt)">
        <img src="#(item.thumbnailURL)" alt="#(item.alt)" loading="lazy">
    </button>
    #endfor
</div>
