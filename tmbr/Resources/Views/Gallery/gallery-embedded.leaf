<div id="gallery-grid">
    #for(image in images):
    <button type="button" class="gallery-item" data-url="#(image.url)" data-alt="#(image.alt)">
        <img src="#(image.thumbnailURL)" alt="#(image.alt)" loading="lazy">
    </button>
    #endfor
</div>
