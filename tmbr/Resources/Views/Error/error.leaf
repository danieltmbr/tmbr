#extend("Shared/page"):
    #export("main"):
        <a href="/">#extend("Shared/signature")</a>
        <article>
            <h1>#(title)</h1>
            #unsafeHTML(message)
            #if(suggestedFixes):
                <ul class="suggested-fix">
                #for(fix in suggestedFixes):
                    <li>#unsafeHTML(fix)</li>
                #endfor
                </ul>
            #endif
        </article>
    #endexport
#endextend

